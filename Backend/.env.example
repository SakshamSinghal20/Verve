# Server
PORT=5000
FRONTEND_URL=https://verve-live.vercel.app
SERVER_IP=127.0.0.1

# MongoDB
# MONGO_URI=mongodb+srv://<user>:<pass>@cluster.mongodb.net/verve

# Auth & Embed JWT
# JWT_SECRET=your_random_secret_string_here

# Phase 1 — Tenant/Embed
# No extra env vars required beyond FRONTEND_URL and JWT_SECRET.
# Tenant API keys are stored in MongoDB and generated at registration time.
